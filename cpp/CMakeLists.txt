if(EIGEN3_INCLUDE_DIR)
  include_directories(${EIGEN3_INCLUDE_DIR})
endif()
if(Boost_INCLUDE_DIR)
  include_directories(${Boost_INCLUDE_DIR})
endif()
if(CFitsIO_INCLUDE_DIR)
  include_directories(${CFitsIO_INCLUDE_DIR})
endif()
if(CCFits_INCLUDE_DIR)
  include_directories(${CCFits_INCLUDE_DIR}/..)
endif()
include_directories(${FFTW3_INCLUDE_DIR})

add_library(cppurify MeasurementOperator.cc)
target_link_libraries(cppurify ${FFTW3_DOUBLE_LIBRARY} ${CCFits_LIBRARY})
add_dependencies(cppurify lookup_dependencies)

if(tests)
  add_subdirectory(tests)
endif()
